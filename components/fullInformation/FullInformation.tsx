import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export default function FullInformation() {
  return (
      <View style={styles.container}>
          <Text>FullInformation</Text>
      </View>
  );
}

const styles = StyleSheet.create({
  container: {
  },
});
